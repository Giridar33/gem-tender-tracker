# --- Web Scraping ---
requests==2.32.3
beautifulsoup4==4.12.3
lxml                      # pre-built wheel; version resolved by pip
tenacity==8.3.0           # exponential-backoff retries
# playwright is listed separately; install manually if needed:
#   pip install playwright && playwright install chromium

# --- Data Processing ---
pandas                    # pre-built wheel
numpy                     # pre-built wheel
python-dateutil==2.9.0

# --- Database ---
sqlalchemy>=2.0.0           # Python 3.13 compatible wheel
# psycopg[binary] — PostgreSQL driver with pre-built wheels (Python 3.13 + Windows compatible)
# Use scheme: postgresql+psycopg:// in DATABASE_URL
psycopg[binary]
alembic>=1.13.0

# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv==1.0.1

# --- Scheduling ---
apscheduler==3.10.4

# --- Testing ---
pytest==8.2.0
httpx==0.27.0              # async test client for FastAPI
responses==0.25.3          # mock HTTP calls

# --- Logging & Utils ---
structlog==24.2.0

# --- Bonus AI/ML (Gemini — FREE tier, no credit card needed) ---
# Get API key at: https://aistudio.google.com/app/apikey
# New google-genai SDK uses v1 API (not v1beta) — supports gemini-1.5-flash correctly
google-genai>=1.0.0

# --- Streamlit Dashboard ---
plotly>=5.22.0            # charts in streamlit_app.py

# NOTE: Streamlit dashboard deps are also in requirements-streamlit.txt
# (used by Streamlit Cloud only — not needed on Railway)
